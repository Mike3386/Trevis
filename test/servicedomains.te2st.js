const domains = require('../services/domain');


describe('domain testing', ()=>{
    test('test', ()=> {
        expect(1).toBe(1);
        //await 

        /*return authService.login({email: 'aa@mail.ru', password:'123'})
        .then((id)=>{expect(id).toBe(1)});    */
    })
});

